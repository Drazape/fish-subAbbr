Library functions called by `sub-abbr` (used as the *Expander* function) for expansion.

# Usage
`sub-abbr add --expander=<FUNC> `(?:`--`)` <args>`

# Individual
## append
Append a given string to the sub-command
### Argument: Positional
**String**: the string that is appended after the sub-command
